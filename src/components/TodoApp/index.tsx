import { Typography } from '@mui/material';

const TodoApp = () => {
  return (
    <div className="w-full flex flex-col items-center justify-center py-36 h-400 bg-amber-500 z-0 ">
      <div className="flex items-center justify-center w-full">
        <Typography className="text-lg text-purple mt-44">TodoApp</Typography>
      </div>
    </div>
  );
};

export default TodoApp;
